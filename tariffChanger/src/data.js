const ACCEPTED = 'Aktivacija tarife prihvaćena.';
const CHANGED = 'Tarifa je aktivirana';
const EXCHANGE = 'tariff';
const QUEUE = 'tariff.changing';

module.exports = {
    ACCEPTED: ACCEPTED,
    CHANGED: CHANGED,
    EXCHANGE: EXCHANGE,
    QUEUE: QUEUE,
}